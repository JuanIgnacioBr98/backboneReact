import { customColors } from "../../themes/customColors";

export const styles = {
  container: {
    textAlign: "center",
  },
  textMessage: {
    color: customColors.primary.primaryBackground,
  },
};
